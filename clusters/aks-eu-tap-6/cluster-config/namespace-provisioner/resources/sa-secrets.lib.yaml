#@ load("@ytt:overlay", "overlay")
#@ def customize():

#! rewrite the default serviceaccount secrets/ requires gitlab-workload-token and github-workload-token secrets from /custom-resources/workload-sa/workload-sa-with-secrets.yaml
#@overlay/match by=overlay.subset({"apiVersion": "v1", "kind": "ServiceAccount","metadata":{"name":"default"}}), expects="0+"
---
# not used anymore , this info is now managed in the tap-values.yaml 
# through namespace_provisioner/default_parameters/supply_chain_service_account[secrets,imagePullSecrets]# 
# https://docs-staging.vmware.com/en/draft/VMware-Tanzu-Application-Platform/1.6/tap/namespace-provisioner-use-case4.html#update-serviceaccount-for-all-namespaces-4
---
#secrets:
#  - name: app-registry
#  - name: git-ssh
#  - name: registries-credentials
#imagePullSecrets:
#  - name: app-registry
#  - name: tap-registry
#@  end
